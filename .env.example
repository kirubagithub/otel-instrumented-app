# OpenObserve OTLP exporter (required for telemetry)
# Example cloud: https://api.openobserve.ai/api/<org>
# Example self-hosted: http://host.docker.internal:5080/api/<org>
OPENOBSERVE_OTLP_ENDPOINT=https://api.openobserve.ai/api/default
# Authorization header value, e.g. Basic <base64(email:password)> or API token
OPENOBSERVE_AUTHORIZATION=Basic REPLACE_ME
OPENOBSERVE_ORG=default
OPENOBSERVE_STREAM=default

# Optional Stripe test mode (leave empty to skip Stripe spans)
STRIPE_SECRET_KEY=

# Frontend → BFF URL (browser-accessible)
VITE_BFF_URL=http://localhost:3000
# RUM OTLP: use /otlp (nginx same-origin proxy to collector). Override only if needed.
VITE_OTEL_EXPORTER_OTLP_ENDPOINT=/otlp
